stages:
  - build

build-job:
  stage: build
  tags:
    - GitLabRunner  # Asegúrate de que esta etiqueta coincida con la del runner
  script:
    - echo "Creando archivo de prueba..."
    # - ssh-keyscan -H 172.19.20.37 > known_hosts
    # - sshpass -p Suhuty1931 scp -o UserKnownHostsFile=known_hosts README.md odoo@172.19.0.37:/opt/odoo/mpz/extra-addons/README.md
  # only:
  #   - main
