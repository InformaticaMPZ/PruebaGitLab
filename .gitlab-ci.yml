stages:
  - build

build-job:
  stage: build
  tags:
    - GitLabRunner  # Asegúrate de que esta etiqueta coincida con la del runner
  script:
    - echo "Creando archivo de prueba..."
    - ssh-keyscan -H 172.19.20.37 > known_hosts
    - sshpass -p "$ODOO_PASSWORD" scp -o UserKnownHostsFile=known_hosts README.md "$ODOO_USER@$ODOO_HOST:$DEST_PATH/README.md"
  only:
    - main
