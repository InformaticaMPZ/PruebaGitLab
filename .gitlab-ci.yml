stages:
  - build

build-job:
  stage: build
  tags:
    - GitLabRunner
  script:
    # - which sshpass || sudo apt-get update && sudo apt-get install -y sshpass
    - ssh-keyscan -H "$ODOO_PRUEBAS_HOST" > known_hosts
    - sshpass -p 'Suhuty1931' scp -v -o UserKnownHostsFile=known_hosts prueba_module mpzadmin@"$ODOO_PRUEBAS_HOST":/opt/odoo/mpz/extra-addons/prueba_module
  only:
    - main
